name: 🜂 Deploy VES Portal

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    name: 🚀 Deploy to GitHub Pages
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4

    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: 📦 Install Dependencies
      run: npm ci

    - name: 🏗️ Build Portal
      run: npm run build
      env:
        NODE_ENV: production

    - name: 🚫 Disable Jekyll
      run: touch dist/.nojekyll

    - name: 📤 Upload Artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./dist

    - name: 🌐 Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4

    - name: 🎉 Deployment Complete
      run: |
        echo "🜂 VES Portal deployed!"
        echo "URL: ${{ steps.deployment.outputs.page_url }}"
